{
  "name": "tesschain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "cd backend && python3 -m uvicorn main:app --reload --port 8000",
    "build:web": "npm run build --prefix apps/web",
    "compile:contracts": "npm run compile --prefix contracts"
  }
}
